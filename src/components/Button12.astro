<button>
  <span class="bg-only"></span>
  <slot />
</button>

<style>
  button {
    --primary-color: #363636;
    --secondary-color: #ff0;
    --duration: 0.5s;
    --delay: 0.4s;
    display: flex;
    position: relative;
    flex: 1;
    flex-direction: column;
    z-index: 0;
    transition: var(--duration) var(--delay);
    cursor: pointer;
    border: none;
    border-radius: 8px;
    background-color: transparent;
    padding: 1rem 2rem;
    color: var(--secondary-color);
    font-size: 2.5rem;
    line-height: 1;

    &::before,
    &::after {
      position: absolute;
      top: 50%;
      transform: translateY(-50%);
      opacity: 0;
      z-index: -1;
      transition: var(--duration);
      transition: 0.3s;
      border-radius: 50%;
      background-color: var(--secondary-color);
      width: 20px;
      height: 20px;
      pointer-events: none;
      content: '';
    }
    &::before {
      left: 0;
      box-shadow: -100px 0 0 var(--secondary-color);
    }
    &::after {
      right: 0;
      box-shadow: 100px 0 0 var(--secondary-color);
    }
    .bg-only {
      position: absolute;
      top: 0;
      left: 0;
      transform: scale(0);
      z-index: -1;
      transition: 0.3s var(--delay);
      border-radius: 8px;
      background-color: var(--secondary-color);
      width: 100%;
      height: 100%;
    }

    &:hover {
      color: var(--primary-color);
      &::before,
      &::after {
        opacity: 1;
      }
      &::before {
        left: 50%;
        transform: translate(-50%, -50%);
        box-shadow: 30px 0 0 var(--secondary-color);
      }
      &::after {
        right: 50%;
        transform: translate(50%, -50%);
        box-shadow: -30px 0 0 var(--secondary-color);
      }
      & .bg-only {
        transform: scale(1);
      }
    }
  }
</style>
