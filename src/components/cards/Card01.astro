<article><slot /></article>

<style>
  article {
    --box-shadow-color: #26262688;
    --primary-color: #26262688;
    --secondary-color: #fafafa;
    --tertiary-color: #ff56ac;
    --duration: 0.5s;
    display: flex;
    position: relative;
    flex-direction: column;
    gap: 1rem;
    z-index: 0;
    cursor: pointer;
    box-shadow: 0 10px 30px var(--box-shadow-color);
    background-color: var(--secondary-color);
    padding: 2rem;
    p {
      align-self: center;
      color: var(--primary-color);
    }
    a {
      align-self: start;
      box-shadow: 0 2px 20px #0000000d;
      background-color: var(--secondary-color);
      padding: 0.5rem;
      font-weight: 600;
    }
    h3,
    p,
    &::after {
      transition: var(--duration);
    }
    &::after {
      position: absolute;
      bottom: 0;
      left: 0;
      z-index: -1;
      background-color: var(--tertiary-color);
      width: 100%;
      height: 5px;
      content: '';
    }

    &:hover {
      h3,
      p {
        color: var(--secondary-color);
      }

      &::after {
        height: 100%;
      }
    }
  }
</style>
