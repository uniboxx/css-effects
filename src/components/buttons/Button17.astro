<button>
  <div><span class="first-letter">B</span><span>utton17</span></div>
  <slot />
</button>

<style>
  button {
    --primary-color: #272727;
    --secondary-color: #f6f6f6;
    --duration: 0.2s;

    display: flex;
    position: relative;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    transition: var(--duration);
    cursor: pointer;
    border: 2px solid var(--primary-color);
    background-color: transparent;
    padding: 0.5rem 1rem;
    color: var(--primary-color);
    font-size: 2.5rem;
    line-height: 1;

    &::before {
      position: absolute;
      top: 0;
      left: 0;
      opacity: 0;
      z-index: -1;
      transition: 0.8s;
      background: var(--primary-color);
      width: 100%;
      height: 100%;
      content: '';
    }
    .first-letter {
      color: transparent;
      &::before {
        position: absolute;
        transition: var(--duration);
        content: 'B';
        color: var(--primary-color);
      }
    }

    &:hover {
      color: var(--secondary-color);
      .first-letter::before {
        transform: translate(-45%, -30%) rotate(360deg);
        color: var(--secondary-color);
        font-size: 5rem;
      }
      &::before {
        transform: rotate(15deg);
        opacity: 1;
      }
    }
  }
</style>
