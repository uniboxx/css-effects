<button>
  <div id="before">Button11 <small>Hover Me</small></div>
  <div id="after">Button11 <small>Hover Me</small></div>
</button>

<style>
  button {
    --primary-color: #363636;
    --secondary-color: #ff0;
    --duration: 0.5s;
    position: relative;
    border: none;
    background-color: #999;
    overflow-x: hidden;

    #before,
    #after {
      display: flex;
      position: absolute;
      top: 0;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      transition: var(--duration);
      cursor: pointer;
      border: 3px solid var(--secondary-color);
      background-color: var(--primary-color);
      width: 100%;
      height: 6rem;
      height: 100%;
      color: var(--secondary-color);
      font-size: 2.5rem;
      line-height: 1;
      small {
        font-size: 1rem;
      }
    }

    #before {
      left: -100%;
      transform: scale(0) rotateY(0deg);
      opacity: 0;
      pointer-events: none;
    }
    #after {
      left: 0;
      /* opacity: 0; */
    }

    &:hover {
      #before {
        left: 0;
        transform: scale(1) rotateY(360deg);
        opacity: 1;
      }
      #after {
        left: 100%;
        transform: scale(0) rotateY(360deg);
        opacity: 0;
      }
    }
  }
</style>
