<a href="#"><slot /><span></span><span></span><span></span><span></span></a>

<style>
  a {
    --primary-color: #ff0;
    --duration: 0.2s;
    display: flex;
    position: relative;
    justify-content: center;
    align-items: center;
    gap: 1rem;
    transition: var(--duration);
    margin: 0 auto;
    padding: 0.5rem 0;
    width: max-content;
    color: inherit;
    font-size: 1.2rem;
    text-decoration: none;

    span {
      position: absolute;
      opacity: 0;
      transition: var(--duration);
      border: 0 solid var(--primary-color);
      width: 10px;
      height: 10px;
      content: '';
      &:nth-child(1),
      &:nth-child(2) {
        top: 9px;
      }
      &:nth-child(1),
      &:nth-child(4) {
        left: -2px;
      }
      &:nth-child(1) {
        border-width: 2px 0 0 2px;
      }
      &:nth-child(2) {
        border-width: 2px 2px 0 0;
      }
      &:nth-child(3),
      &:nth-child(4) {
        bottom: 9px;
      }
      &:nth-child(2),
      &:nth-child(3) {
        right: -2px;
      }
      &:nth-child(3) {
        border-width: 0 2px 2px 0;
      }
      &:nth-child(4) {
        border-width: 0 0 2px 2px;
      }
    }

    &:hover {
      color: var(--primary-color);
      span {
        opacity: 1;
        &:nth-child(1),
        &:nth-child(2) {
          top: 0;
        }
        &:nth-child(3),
        &:nth-child(4) {
          bottom: 0;
        }
        &:nth-child(1) {
          left: -8px;
        }
        &:nth-child(2) {
          right: -8px;
        }
        &:nth-child(3) {
          right: -8px;
        }
        &:nth-child(4) {
          left: -8px;
        }
      }
    }
  }
</style>
